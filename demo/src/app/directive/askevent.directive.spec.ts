import { AskeventDirective } from './askevent.directive';

describe('AskeventDirective', () => {
  it('should create an instance', () => {
    const directive = new AskeventDirective();
    expect(directive).toBeTruthy();
  });
});
